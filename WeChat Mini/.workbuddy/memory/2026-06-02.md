# 2026-06-02 工作日志

## We 分析性能优化（7项建议全部实施）

### P0 - 已修复
- **图片尺寸优化**：修复 waterfall 组件 WXS `optimizeUrl` 定义但未调用的 bug，src 改为 `{{utils.optimizeUrl(item.coverUrl or item.url, item.resourceType or item.type)}}`；同时改进 WXS 支持 GIF 保护 + WebP 格式转换
- **网络超时配置**：project.config.json 添加 `networkTimeout`（request 15s, download 30s）；wx.request/wx.downloadFile 关键调用添加 timeout 参数；创建 `utils/retry.js` 重试工具模块
- **激励视频广告修复**：ad-unit.js 添加 generation 计数器防止旧实例 onClose/onError 回调污染新广告；load() 失败添加递增延迟重试（最多3次）；preview.js/wallpaper-preview.js 区分"未完整观看"和"加载失败"的提示文案

### P1 - 已修复
- **启动性能**：`_preheatHomeData` 从同步调用改为 `wx.nextTick` 延迟执行；拆分 storage key 为关键（TOKEN/USER_INFO/OPENID 立即预热）和非关键（其余 key 延迟 2s）
- **同步 Storage 回退移除**：`getStorage()` 完全移除 `wx.getStorageSync` 同步路径，仅依赖异步预热 + 内存缓存；删除 `_isStartupPhase` 变量

### P2 - 已修复
- **重复 storage 读取合并**：profile.js `syncUserInfo()` 复用 `this.data.userInfo`，避免冗余的 getStorage + setData

### P3 - 待验证
- **HTTP/2**：微信客户端自动处理，云开发 CDN 默认支持（CDN 域名 tcb.qcloud.la 需在控制台确认 HTTP/2 已启用）

### 激励视频广告 RCA
- **拉取不到**：load() 失败无重试 → 添加 3 次递增延迟重试
- **中途退出再点提示"需完整观看"**：旧 ad 实例的 onClose 异步延迟触发，拿到新 ad 的 _adResolve 污染 Promise → 添加 _adGeneration 计数器，旧回调直接跳过

### 修改文件（12个）
app.js, storageManager.js, waterfall.wxml, wallpaper.js, avatar.js, profile.js, project.config.json, home.js, preview.js, wallpaper-preview.js, ad-unit.js, retry.js(新)
